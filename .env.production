# Production build for explorer.zkas.info.
# The SPA is served same-origin; nginx reverse-proxies /api → zkas-api (:8500).
VITE_API_BASE=/api
VITE_SHIELDED_API_BASE=/api
VITE_SOCKET_URL=wss://explorer.zkas.info
